<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD</title>
    <link rel="stylesheet" href="/styles/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a href="/read" class="text-blue-500">Update</a>
        </div>
                <h1 class="px-5 py-2 flex justify-center font-bold text-white text-3xl tracking-tighter mb-3">Cards</h1>
            <div class="flex justify-center ">
                <form action="/update/<%= cards._id%>" method="post">
                    <input type="text" placeholder="name" value="<%= cards.name%>" name="name" class="px-5 py-2 border-2 border-zinc-600 rounded-lg outline-none bg-transparent"> 
                    <input type="text" placeholder="Email" name="email" value="<%= cards.email%>" class="px-5 py-2 border-2 border-zinc-600 rounded-lg outline-none bg-transparent">
                    <input type="text" placeholder="Image Url" name="image" value="<%= cards.image%>" class="px-5 py-2 border-2 border-zinc-600 rounded-lg outline-none bg-transparent">
                    <input type="submit" value="Update Card" class="px-5 py-2 border-2 bg-yellow-600 rounded-lg ml-3 border-blue-600"> 
                </form>
            </div>
    </div>
</body>
</html>